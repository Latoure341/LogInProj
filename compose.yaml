
services:
  backend:
    build:
      context: ./Backend
      dockerfile: Dockerfile
    restart: unless-stopped
    volumes:
      - ./Backend:/app
      - /app/node_nodules
    ports:
      - "5000:5000"
    environment:
    - PORT=5000
    - 'mongodb+srv://tukamothupi_db_user:2FStjH8gT1rDX0WW@thedeveloper.bkgwozo.mongodb.net/?appName=TheDeveloper'
    depends_on:
      - db

  db:
    image: mongo:6
    restart: always
    volumes:
      - shared-data:/data/db

volumes:
  shared-data: {}
